# personal Hooks

Ejemplo:

´´´

# useForm

    const initialForm = {
        neme:'',
        age:0,
        email:''
    }
    const [formValues, handleInputChanche, reset] = useForm(initialForm);

´´´
